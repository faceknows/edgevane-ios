---
description: money-ios 必须按需求→设计→实现门禁推进，禁止跳步写代码。
alwaysApply: true
---

# Docs-first

产品工作遵循 `Docs/ExecutablePlan.md`。同级仓库 `../money-app` 是 React Native 参考实现，不是产品定义；验收后的需求才是产品定义。

## 门禁

1. 需求与 `Docs/design/` 功能篇已验收。实现对照设计，不要另起接口或再发明后端。
2. 按 ExecutablePlan 的切片推进。一次只做一个可交付切片。
3. 验收看用户可感知结果，不是“文件写完了”。
4. 默认复用现有后端。新增、删除、优化 API 只能写在需求的「后端提案」里，经用户确认后再进设计。

## 写作约束

- 需求写产品行为，不写类名、文件夹、第三方库调用细节。
- 设计写模块边界与落地方式，不改已验收需求。
- 实现对照 `Docs/design/`，不要另起一套接口或再发明后端。
