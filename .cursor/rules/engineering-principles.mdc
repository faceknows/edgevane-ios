---
description: 工程原则：可扩展、图表与组件复用；细节见需求 09。
alwaysApply: true
---

# 工程原则

写设计或代码时遵守 `Docs/requirements/09-工程与扩展原则.md`。

- 页面不打 URL、不算 VWAP、不直接调图表库。
- 主图一个能力多用；图表只吃棒 / 折线 / 价格线。
- 扫描器、股票行、空态、交易确认必须复用，禁止复制页。
- 第一期可以少做功能，不能把结构做成只能粘贴才能加功能。
- 不预埋付费墙或 Google 登录空按钮。
- 交易/持仓/订单按「当前券商账户」建模，不要写成用户身上唯一的 Alpaca Key。多券商（Schwab、IBKR 等）只预留，第一期不实现、不预埋「添加券商」按钮。
- 不发明后端路径。
