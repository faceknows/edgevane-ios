# 待确认事项

**状态：** 待你拍板  

请直接在本页改「决定」列，或在对话里按编号回复。没勾选的提案按「不采纳 / 后做」处理。

## 已确认（本次 review）

| 项 | 决定 |
| --- | --- |
| 付费用户 / IAP / 套餐墙 / 升级 | **不做**，不作为 App 需求 |
| Google / Apple / 第三方登录 | **不做**，不作为 App 需求 |
| 准入 | **只有受邀请用户**：邀请码 + 邮箱注册；已有账号只用邮箱密码登录 |
| 实时标的数量 | 不按免费 / Pro 限额 |
| 应用身份 | **直接替换现网 App**：展示名 `moneyknows`，生产 Bundle ID `com.byteknows.moneyknows` |
| 从 RN 覆盖安装后的登录态 | 第一期不承诺无感迁移；用户需重新登录并重填 Alpaca 凭证 |
| 工程原则 | 见 [09-工程与扩展原则.md](09-工程与扩展原则.md) |
| 多券商账户 | **设计预留，第一期不实现。** 见 [10-多券商账户.md](10-多券商账户.md) |
| 下单价 | **第一期就要 bid/ask**；没有才退回最新成交 |
| 忘记密码 | 行业惯例：验证码 + 新密码 + 确认密码 → **回登录页再用新密码登录**，不自动进 App |
| 注册邮箱验证码 | **不要**。注册只要邀请码 + 邮箱密码（确认、至少 8 位） |
| 已订阅盘口 | socket `quote` 为主，**快照兜底**（字段 `bp` / `ap`） |
| VXX | **从产品拿掉**；纳指可保留为独立矮图 |
| 现网付费 | **目前没有真正付费用户**，去掉 IAP 无历史订阅包袱 |
| 邀请码 | **运营线下发**；第一期 App 内不做邀请入口 |

## A. 范围

| # | 问题 | 建议 | 决定（空着） |
| --- | --- | --- | --- |
| A1 | 隐藏扫描器第一期做不做？ | 不做 | **按建议** |
| A2 | 微观分析第一期做不做？ | 不做 | **按建议** |
| A3 | Admin（含 App 内发邀请）第一期做不做？ | 不做；邀请码线下发 | **已确认** |
| A4 | 未订阅是否出交易条/秒图？ | 仅已订阅 | **按建议** |
| A5 | 详情是否要 RSI/MACD 副图？ | 只要徽标 | **按建议** |
| A6 | 日线与分钟线同步十字光标？ | 不要 | **按建议** |

## B. 交易

| # | 问题 | 建议 | 决定 |
| --- | --- | --- | --- |
| B1 | 仅网关推送时自动止盈和止损都要生效？ | 都要 | **按建议** |
| B2 | 开盘保护默认 30 分钟？ | 保持，可在偏好改 | **按建议** |
| B3 | 组合是否必须开盘 1 秒刷新？ | 开盘 3s / 收盘 15s | **按建议** |

## C. 后端提案（默认全否，未另说则按建议）

| # | 提案 | 建议 | 决定 |
| --- | --- | --- | --- |
| C1 | 订单推送只保留一条通道 | 第一期客户端自己去重，后端后改 | **按建议** |
| C2 | 订阅 REST 并到 money-api | 否 | **按建议** |
| C3 | 真正的 staging 环境 | 若你要 TestFlight 隔离再做 | **按建议** |
| C4 | 分钟线用 `bar` 事件增量 | 否（可后做优化） | **按建议** |
| C5 | 恢复微观分析 Socket | 等 A2 为「做」再开 | **按建议** |
| C6 | 交易计划 AI 改服务端 | 否（第一期不做该功能） | **按建议** |
| C7 | Alpaca 凭证上云 | 否 | **按建议** |
| C8 | 统一 `{ data }` 信封 | 否 | **按建议** |
| C9 | Socket 握手带 App 版本 | 否 | **按建议** |

## D. 产品与品牌

| # | 问题 | 建议 | 决定 |
| --- | --- | --- | --- |
| D1 | iOS 应用展示名是否仍为 moneyknows？ | 是 | **已确认** |
| D2 | Bundle ID / 是否沿用现有 App Store 应用，还是新应用？ | 沿用 `com.byteknows.moneyknows`，作为现网更新上架 | **已确认** |
| D3 | 最低 iOS 版本 15 是否可接受？ | 是（图表库要求） | **按建议** |
| D4 | 中文是否以简体为主（RN `zh`）？ | 是 | **按建议** |

## E. 验收方式

需求与功能设计已按本页「已确认」收口。未另作决定的条目按「建议 / 按建议」执行。

要开工时回复：「设计可以，开始实现」。

## F. 实现默认（已按建议收口）

| # | 问题 | 决定 |
| --- | --- | --- |
| F1 | iPad / 横屏 | 第一期只做 **iPhone 竖屏** |
| F2 | 详情「今天」主图是否叠今日成交 | **做**，与复盘共用同一套标记 |
| F3 | 改单还能改哪些字段 | 价格和/或数量；其余跟 Alpaca `replace` |
| F4 | 注册密码 | 确认密码 + 至少 8 位 |
| F5 | 持仓平仓 | 受开盘保护；市价全平跟 `showMarketTrade` |
| F6 | 自动止盈止损 vs 开盘保护 | 保护窗内不挂，失败提示 |
| F7 | 复盘点 | 一笔已成交订单一个点，不拆部分成交 |
